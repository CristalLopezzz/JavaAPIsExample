public class YelpSearch {
    public static void main(String[] args) {
        // I made a yelp account and tried making a new app like shown in the video
        // But it wouldn't let me make it because it required me to use a business email.
        // I tried using my personal email and it did not work.
        // I still watched the video and followed along with the code
        // This is the url for the yelp API
        // String yelpURL = "url"

        // String YELP_API_KEY = "url from the website API key from the app"

        // Parameters for the yelp query
        //Map<String, Object> yelpQuery = Map.of(
        //"term", "pizza",
        // "location", "Minneapolis,MN",
        //"categories", "restaurants",
        // "price", "1");

        // Get request for the Yelp API
        // YelpResponse = Unirest
        // .get(yelpURL)
        //.header("Authorization", "Bearer " + YELP_API_KEY)
        // .queryString(yelpQuery)
        // .asObject(YelpResponse.class).getBody();

// print result
        // System.out.println(response);
            //for(Business b: response.businesses) {
                //System.out.println(b.name);
                //System.out.println("Rating" + b.rating);
                // System.out.println(b.location.address1 + ", " + b.location.city);
    }
}
